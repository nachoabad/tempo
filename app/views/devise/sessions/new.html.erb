<section class="ftco-section">
  <div class="container">
    <div class="row justify-content-center mb-3">
      <div class="col-lg-4 col-md-6">
        <div class="text-center heading-section">
          <% auth_path = resource_name == :admin ? '/admins/auth/admin_google' : '/users/auth/user_google' %>
          <%= link_to auth_path, class: 'btn btn-primary btn-block py-3 px-5' do %>
            <i class="fa fa-google"></i> Iniciar con Google
          <% end %>
        </div>
      </div>
    </div>
    <div class="row justify-content-center">
      <div class="col-lg-4 col-md-6">
        <p class="text-center">o con tu email</p>
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="form-group">
            <%= f.email_field :email, class: 'form-control', autocomplete: "email", placeholder: 'Email' %>
          </div>
          <div class="form-group">
            <%= f.password_field :password, class: 'form-control', autocomplete: "current-password", placeholder: 'Clave' %>
          </div>
          <div class="form-group">
            <input type="submit" value="Iniciar sesión" class="btn btn-primary btn-block py-3 px-5">
          </div>
        <% end %>
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</section>